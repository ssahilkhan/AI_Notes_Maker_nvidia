# Get a free API key at https://build.nvidia.com/nvidia/nemotron-3-ultra-550b-a55b
# This is the SHARED workspace key: every user's responses use it by default.
# Individual users can optionally add their own key in the sidebar Settings;
# their own key then takes precedence for their responses.
NVIDIA_API_KEY=nvapi-YOUR_KEY_HERE